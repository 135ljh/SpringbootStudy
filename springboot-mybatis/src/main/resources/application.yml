# datasource
spring:
  datasource:
    url: jdbc:mysql:///db1
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

# mybatis
mybatis:
  mapper-locations: classpath:mapper/*.xml # mapper映射文件路径
  type-aliases-package: com.ljh.springbootmybatis.domain
  # config-location: # 指定mybatis的核心配置文件